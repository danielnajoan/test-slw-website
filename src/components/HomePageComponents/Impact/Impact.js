import React, { memo } from 'react';
import {
    BaseSection,
    Headline,
    ImageContainer,
} from './Dial.elements';

function Impact() {
    return (
        <>
            
        </>
    )
};

export default memo(Impact);
